import{Box,Button,Stack,Typography}from'@mui/material';import AddIcon from'@mui/icons-material/Add';import RefreshIcon from'@mui/icons-material/Refresh';
export default function PageHeader({title,description,onAdd,onRefresh,addLabel}:{title:string;description:string;onAdd?:()=>void;onRefresh?:()=>void;addLabel?:string}){return <Stack direction={{xs:'column',sm:'row'}} justifyContent="space-between" alignItems={{sm:'center'}} spacing={2} mb={3}><Box><Typography variant="h4">{title}</Typography><Typography color="text.secondary" mt={.5}>{description}</Typography></Box><Stack direction="row" spacing={1}>{onRefresh&&<Button variant="outlined" startIcon={<RefreshIcon/>} onClick={onRefresh}>Refresh</Button>}{onAdd&&<Button variant="contained" startIcon={<AddIcon/>} onClick={onAdd}>{addLabel||'Add new'}</Button>}</Stack></Stack>}
